services:

  api:
    build: .
    container_name: rate_limiter_api
    ports:
      - "8000:8000"
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: rate_limiter_redis
    ports:
      - "6379:6379"