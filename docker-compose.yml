version: "3.9"

services:

  api:
    build: .
    container_name: rate_limiter_api
    ports:
      - "8000:8000"
    depends_on:
      - redis
    restart: always

  redis:
    image: redis:7
    container_name: rate_limiter_redis
    ports:
      - "6379:6379"
    restart: always